import React from 'react'
import './PositionPrimary.css'
export default function PositionPrimary (props) {
	return (
		<div className={`PositionPrimary_PositionPrimary ${props.className}`}>
		</div>
	)
}
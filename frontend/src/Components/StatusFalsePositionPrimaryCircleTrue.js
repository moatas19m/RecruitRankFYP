import React from 'react'
import './StatusFalsePositionPrimaryCircleTrue.css'
export default function StatusFalsePositionPrimaryCircleTrue (props) {
	return (
		<div className={`StatusFalsePositionPrimaryCircleTrue_StatusFalsePositionPrimaryCircleTrue ${props.className}`}>
		</div>
	)
}